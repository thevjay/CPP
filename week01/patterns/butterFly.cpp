#include <iostream>
using namespace std;

int main(){

    int n;
    cin>>n;
    
    for(int row=0;row<n;row++){
        int cond = row <n ? row : 0;
        int space_count = 
        for (int col = 0; col < 2*n; col++)
        {
            cout<<"*";
        }
        cout<<endl;
    }
    return 0;
}